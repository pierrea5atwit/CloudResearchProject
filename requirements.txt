# Core numerical + data
numpy==1.26.4
pandas==2.2.2
PyYAML==6.0.2

# GPU compute (optional but recommended for virtual experiments)
# Choose based on your CUDA version: cupy-cuda11x, cupy-cuda12x, etc.
# For now, leaving as optional - install manually when ready
# cupy-cuda12x>=12.0

# Visualization
matplotlib==3.8.4

# GPU monitoring
pynvml==11.5.0
nvidia-ml-py3==11.5.0

# RAPIDS cuML (NOT installed - use numpy fallback + gpu_kernel workload instead)
# NOTE: Do NOT pip install cuML directly on your system
